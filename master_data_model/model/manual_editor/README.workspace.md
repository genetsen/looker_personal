# Manual Editor Workspace Notes

This folder keeps the Manual Package Editor branch together: user docs, QA, loader, Apps Script source, repair tools, tests, and manual landing-table schema.

The original `manual_package_edits/` folder remains in place for compatibility while this workspace is being adopted.
